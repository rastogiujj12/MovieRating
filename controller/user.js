const Joi = require("@hapi/joi");

const login = async (req, res, next) => {
    //TODO 
    next()
};

const signup = async (req, res, next) =>{
    //TODO 
    next()
}

module.exports = {
  login,
  signup
};
